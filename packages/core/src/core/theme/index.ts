import type { ZyyvTheme } from '../../types'

// IN-README-START
// See index.test.ts `themeAnimate configuration` for usage.
export function nomarlizeTheme(theme: ZyyvTheme): ZyyvTheme {
  return {
    ...theme,
  }
}

// IN-README-END
